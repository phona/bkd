---
id: DS-001
title: Design constitution landing — semantic color + motion tokens
status: in_progress
priority: P1
owner: claude
created: 2026-06-06
updated: 2026-06-06
plan: PLAN-028
---

# DS-001 — Design constitution landing (semantic color + motion tokens)

Land the mechanical, app-wide token foundation from DESIGN.md pillars A + D.
Full design in PLAN-028.

## Steps

1. **index.css token layer** (this commit): add `--accent-brand` (teal) +
   `--accent-brand-foreground`; semantic status tokens
   (`--success/--warning/--info/--neutral`); motion tokens
   (`--duration-*`, `--ease-*`); expose via `@theme inline`; add a
   `prefers-reduced-motion` guard. All additive — no consumer changes yet.
2. **statuses.ts** references the semantic tokens instead of raw hex.
3. **Sweep** `transition-all` → specific `transition-colors/-transform/-opacity`.
4. **Dedup** keyframes (single thinking indicator).

## Verification

`bun --filter @bkd/frontend lint` + `tsc --noEmit` + frontend test suite.
